<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edtting</title>
    <link rel="stylesheet" href="/styles.css" />

</head>
<body>
    <h1>Id: <%=User.id %></h1>
   
    <form method="post" action="/FinishEdit/<%=User.id %>" class="ButtonHolder">
        <label>Name:</label>
        <input type="text" name="Name" minlength="5"  maxlength="30" required value="<%=User.Name %>"/>
        <label>Phone:</label>
        <input type="tel" name="Phone" minlength="5"  maxlength="30" required value="<%=User.Phone %>"/>
        <label>Address:</label>
        <input type="text" name="Address" minlength="5"  maxlength="30" requiredvalue="<%=User.Address %>"/>
        <label>Age:</label>
        <input type="number" name="Age" max="100" min="10" required value="<%=User.Age %>"/>
        <label>Password:</label>
        <input type="password" name="Password" minlength="5" maxlength="30" required value="<%=User.Password %>"/>
        <br>
        <input type="submit" value="Save" />
    </form>
    <a href="/">Home</a>

</body>
</html>